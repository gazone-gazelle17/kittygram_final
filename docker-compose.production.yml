version: '3'

volumes:
  pg_data:
  static:
  media:

services:
  db:
    image: postgres:13
    env_file: .env
    volumes:
      - pg_data:/var/lib/postgresql/data
  backend:
    env_file: .env
    depends_on:
      - db
    volumes:
      - media:/app/media
      - static:/app/static
  frontend:
    env_file: .env
    command: cp -r /app/build/. /static/
    volumes:
      - static:/static
  gateway:
    env_file: .env
    depends_on:
      - backend
    ports:
      - 80:80
    volumes:
      - static:/static
      - media:/app/media
